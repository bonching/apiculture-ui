
# apiculture-ui

## Install the dependencies
Run `npm i` to install the dependencies.

## Configure the API host
- open the file util/ApiRoutes.js
- update the IP address of the API host
```
const API_SERVER = 'http://<IP_ADDRESS>:8080/api'
```

## Running the code
Run `npm run-script dev` to start the development server.
